apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-hdd-pvc
  namespace: ai-stack
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""  # Must be explicitly empty for static PV binding
  resources:
    requests:
      storage: 2Ti
  volumeName: backup-hdd-pv
